<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- Label: Enter time of day -->
    <TextView
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:layout_width="wrap_content"
        android:hint="Enter your time of day:"
        android:textSize="20sp"
        android:layout_gravity="center" />

    <!-- Label: Input field to enter time of day -->
    <EditText
        android:id="@+id/inputTimeOfDay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Insert time of day here..."
        android:inputType="text"
        android:layout_gravity="center" />

    <!-- Label: Button used to determine meal suggestion -->
    <Button
        android:id="@+id/determineMealButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Find Meal"
        android:layout_marginTop="30dp"
        android:layout_gravity="center" />

    <!-- Label: Text box to display meal suggestions -->
    <TextView
        android:id="@+id/mealSuggestionsBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="60dp"
        android:hint="Your meal suggestion will appear here"
        android:textSize="19sp"
        android:background="#E0E0E0"
        android:padding="60dp"
        android:gravity="center"
        android:visibility="visible" />

    <!-- Label: Button to reset given suggestions -->
    <Button
        android:id="@+id/resetButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="140dp"
        android:text="Reset" />

    <!-- Label: Button to exit from the app -->
    <Button
        android:id="@+id/exitButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Exit"
        android:layout_gravity="bottom"
        android:layout_marginTop="50dp" />


</LinearLayout>